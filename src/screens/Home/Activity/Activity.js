import React from "react";
import { View, Text } from "react-native";
import RIP from "../components/RIP";

const Activity = ({ navigation }) => <RIP navigation={navigation} />;

export default Activity;
