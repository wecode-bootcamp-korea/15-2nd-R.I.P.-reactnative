import React from "react";
import { View, Text, ScrollView } from "react-native";
import MiddleTab from "../../../navigations/MiddleTab";

export default () => (
  <ScrollView>
    <MiddleTab />
  </ScrollView>
);
